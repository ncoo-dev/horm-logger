<?php

use Illuminate\Support\Facades\Schema;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Database\Migrations\Migration;

class CreateHormEntriesTable extends Migration
{
    public function up()
    {
        Schema::connection(config('horm.database_connection'))->create(config('horm.table_name'), function (Blueprint $table) {
            $table->uuid();
            $table->string('direction')->default('outgoing');
            $table->string('url')->nullable();
            $table->string('type')->default('response');
            $table->string('method');
            $table->text('request')->nullable();
            $table->text('response')->nullable();
            $table->integer('status_code')->nullable();
            $table->string('content')->nullable();

            $table->timestamps();
            $table->index('url');
            $table->index('method');
            $table->index('status_code');
            $table->index('type');

        });
    }

    public function down()
    {
        Schema::connection(config('horm.database_connection'))->dropIfExists(config('horm.table_name'));
    }
}
